server:
  port: 8080
  tomcat:
    max-swallow-size: -1
    connection-timeout: 600000
    threads:
      max: 200
      min-spare: 20
    accept-count: 200
  compression:
    enabled: true
    mime-types: text/html,text/xml,text/plain,text/css,application/json,application/javascript,application/xml
    min-response-size: 1024

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tech_analysis?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
    username: root
    password: zc20050629
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 60000
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
      file-size-threshold: 10MB
      location: ${java.io.tmpdir}

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.techanalysis.entity
  configuration:
    map-underscore-to-camel-case: true
    # 生产环境请避免控制台SQL日志，必要时使用独立日志文件

jwt:
  secret: tech-analysis-platform-secret-key-2024-very-long-secret
  expiration: 86400000

logging:
  level:
    com.techanalysis: INFO
    root: INFO
